{
  "name": "auto-update-sample",
  "version": "1.0.6",
  "description": "Auto update test",
  "main": "index.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder build --win --publish never",
    "deploy": "electron-builder build --win --publish always",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gerson Jaber",
  "license": "ISC",
  "devDependencies": {
    "electron": "^9.2.0",
    "electron-builder": "^22.8.0"
  },
  "dependencies": {
    "electron-updater": "^4.3.4"
  },
  "repository": {
    "type" : "git",
    "url" : "https://github.com/gjaber/electron-autoupdater-example.git"
  }
  {
    "build":
      "publish": {
        "provider": "s3",
        "bucket": "neo.desktop.update"
      }
    }
}
